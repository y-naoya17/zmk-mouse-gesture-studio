// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v1.180.0
//   protoc               v7.34.1
// source: core.proto
/* eslint-disable */
import * as _m0 from "protobufjs/minimal";
export const protobufPackage = "zmk.core";
export var LockState;
(function (LockState) {
    LockState[LockState["ZMK_STUDIO_CORE_LOCK_STATE_LOCKED"] = 0] = "ZMK_STUDIO_CORE_LOCK_STATE_LOCKED";
    LockState[LockState["ZMK_STUDIO_CORE_LOCK_STATE_UNLOCKED"] = 1] = "ZMK_STUDIO_CORE_LOCK_STATE_UNLOCKED";
    LockState[LockState["UNRECOGNIZED"] = -1] = "UNRECOGNIZED";
})(LockState || (LockState = {}));
export function lockStateFromJSON(object) {
    switch (object) {
        case 0:
        case "ZMK_STUDIO_CORE_LOCK_STATE_LOCKED":
            return LockState.ZMK_STUDIO_CORE_LOCK_STATE_LOCKED;
        case 1:
        case "ZMK_STUDIO_CORE_LOCK_STATE_UNLOCKED":
            return LockState.ZMK_STUDIO_CORE_LOCK_STATE_UNLOCKED;
        case -1:
        case "UNRECOGNIZED":
        default:
            return LockState.UNRECOGNIZED;
    }
}
export function lockStateToJSON(object) {
    switch (object) {
        case LockState.ZMK_STUDIO_CORE_LOCK_STATE_LOCKED:
            return "ZMK_STUDIO_CORE_LOCK_STATE_LOCKED";
        case LockState.ZMK_STUDIO_CORE_LOCK_STATE_UNLOCKED:
            return "ZMK_STUDIO_CORE_LOCK_STATE_UNLOCKED";
        case LockState.UNRECOGNIZED:
        default:
            return "UNRECOGNIZED";
    }
}
function createBaseRequest() {
    return { getDeviceInfo: undefined, getLockState: undefined, lock: undefined, resetSettings: undefined };
}
export const Request = {
    encode(message, writer = _m0.Writer.create()) {
        if (message.getDeviceInfo !== undefined) {
            writer.uint32(8).bool(message.getDeviceInfo);
        }
        if (message.getLockState !== undefined) {
            writer.uint32(16).bool(message.getLockState);
        }
        if (message.lock !== undefined) {
            writer.uint32(24).bool(message.lock);
        }
        if (message.resetSettings !== undefined) {
            writer.uint32(32).bool(message.resetSettings);
        }
        return writer;
    },
    decode(input, length) {
        const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
        let end = length === undefined ? reader.len : reader.pos + length;
        const message = createBaseRequest();
        while (reader.pos < end) {
            const tag = reader.uint32();
            switch (tag >>> 3) {
                case 1:
                    if (tag !== 8) {
                        break;
                    }
                    message.getDeviceInfo = reader.bool();
                    continue;
                case 2:
                    if (tag !== 16) {
                        break;
                    }
                    message.getLockState = reader.bool();
                    continue;
                case 3:
                    if (tag !== 24) {
                        break;
                    }
                    message.lock = reader.bool();
                    continue;
                case 4:
                    if (tag !== 32) {
                        break;
                    }
                    message.resetSettings = reader.bool();
                    continue;
            }
            if ((tag & 7) === 4 || tag === 0) {
                break;
            }
            reader.skipType(tag & 7);
        }
        return message;
    },
    fromJSON(object) {
        return {
            getDeviceInfo: isSet(object.getDeviceInfo) ? globalThis.Boolean(object.getDeviceInfo) : undefined,
            getLockState: isSet(object.getLockState) ? globalThis.Boolean(object.getLockState) : undefined,
            lock: isSet(object.lock) ? globalThis.Boolean(object.lock) : undefined,
            resetSettings: isSet(object.resetSettings) ? globalThis.Boolean(object.resetSettings) : undefined,
        };
    },
    toJSON(message) {
        const obj = {};
        if (message.getDeviceInfo !== undefined) {
            obj.getDeviceInfo = message.getDeviceInfo;
        }
        if (message.getLockState !== undefined) {
            obj.getLockState = message.getLockState;
        }
        if (message.lock !== undefined) {
            obj.lock = message.lock;
        }
        if (message.resetSettings !== undefined) {
            obj.resetSettings = message.resetSettings;
        }
        return obj;
    },
    create(base) {
        return Request.fromPartial(base ?? {});
    },
    fromPartial(object) {
        const message = createBaseRequest();
        message.getDeviceInfo = object.getDeviceInfo ?? undefined;
        message.getLockState = object.getLockState ?? undefined;
        message.lock = object.lock ?? undefined;
        message.resetSettings = object.resetSettings ?? undefined;
        return message;
    },
};
function createBaseResponse() {
    return { getDeviceInfo: undefined, getLockState: undefined, resetSettings: undefined };
}
export const Response = {
    encode(message, writer = _m0.Writer.create()) {
        if (message.getDeviceInfo !== undefined) {
            GetDeviceInfoResponse.encode(message.getDeviceInfo, writer.uint32(10).fork()).ldelim();
        }
        if (message.getLockState !== undefined) {
            writer.uint32(16).int32(message.getLockState);
        }
        if (message.resetSettings !== undefined) {
            writer.uint32(32).bool(message.resetSettings);
        }
        return writer;
    },
    decode(input, length) {
        const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
        let end = length === undefined ? reader.len : reader.pos + length;
        const message = createBaseResponse();
        while (reader.pos < end) {
            const tag = reader.uint32();
            switch (tag >>> 3) {
                case 1:
                    if (tag !== 10) {
                        break;
                    }
                    message.getDeviceInfo = GetDeviceInfoResponse.decode(reader, reader.uint32());
                    continue;
                case 2:
                    if (tag !== 16) {
                        break;
                    }
                    message.getLockState = reader.int32();
                    continue;
                case 4:
                    if (tag !== 32) {
                        break;
                    }
                    message.resetSettings = reader.bool();
                    continue;
            }
            if ((tag & 7) === 4 || tag === 0) {
                break;
            }
            reader.skipType(tag & 7);
        }
        return message;
    },
    fromJSON(object) {
        return {
            getDeviceInfo: isSet(object.getDeviceInfo) ? GetDeviceInfoResponse.fromJSON(object.getDeviceInfo) : undefined,
            getLockState: isSet(object.getLockState) ? lockStateFromJSON(object.getLockState) : undefined,
            resetSettings: isSet(object.resetSettings) ? globalThis.Boolean(object.resetSettings) : undefined,
        };
    },
    toJSON(message) {
        const obj = {};
        if (message.getDeviceInfo !== undefined) {
            obj.getDeviceInfo = GetDeviceInfoResponse.toJSON(message.getDeviceInfo);
        }
        if (message.getLockState !== undefined) {
            obj.getLockState = lockStateToJSON(message.getLockState);
        }
        if (message.resetSettings !== undefined) {
            obj.resetSettings = message.resetSettings;
        }
        return obj;
    },
    create(base) {
        return Response.fromPartial(base ?? {});
    },
    fromPartial(object) {
        const message = createBaseResponse();
        message.getDeviceInfo = (object.getDeviceInfo !== undefined && object.getDeviceInfo !== null)
            ? GetDeviceInfoResponse.fromPartial(object.getDeviceInfo)
            : undefined;
        message.getLockState = object.getLockState ?? undefined;
        message.resetSettings = object.resetSettings ?? undefined;
        return message;
    },
};
function createBaseGetDeviceInfoResponse() {
    return { name: "", serialNumber: new Uint8Array(0) };
}
export const GetDeviceInfoResponse = {
    encode(message, writer = _m0.Writer.create()) {
        if (message.name !== "") {
            writer.uint32(10).string(message.name);
        }
        if (message.serialNumber.length !== 0) {
            writer.uint32(18).bytes(message.serialNumber);
        }
        return writer;
    },
    decode(input, length) {
        const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
        let end = length === undefined ? reader.len : reader.pos + length;
        const message = createBaseGetDeviceInfoResponse();
        while (reader.pos < end) {
            const tag = reader.uint32();
            switch (tag >>> 3) {
                case 1:
                    if (tag !== 10) {
                        break;
                    }
                    message.name = reader.string();
                    continue;
                case 2:
                    if (tag !== 18) {
                        break;
                    }
                    message.serialNumber = reader.bytes();
                    continue;
            }
            if ((tag & 7) === 4 || tag === 0) {
                break;
            }
            reader.skipType(tag & 7);
        }
        return message;
    },
    fromJSON(object) {
        return {
            name: isSet(object.name) ? globalThis.String(object.name) : "",
            serialNumber: isSet(object.serialNumber) ? bytesFromBase64(object.serialNumber) : new Uint8Array(0),
        };
    },
    toJSON(message) {
        const obj = {};
        if (message.name !== "") {
            obj.name = message.name;
        }
        if (message.serialNumber.length !== 0) {
            obj.serialNumber = base64FromBytes(message.serialNumber);
        }
        return obj;
    },
    create(base) {
        return GetDeviceInfoResponse.fromPartial(base ?? {});
    },
    fromPartial(object) {
        const message = createBaseGetDeviceInfoResponse();
        message.name = object.name ?? "";
        message.serialNumber = object.serialNumber ?? new Uint8Array(0);
        return message;
    },
};
function createBaseNotification() {
    return { lockStateChanged: undefined };
}
export const Notification = {
    encode(message, writer = _m0.Writer.create()) {
        if (message.lockStateChanged !== undefined) {
            writer.uint32(8).int32(message.lockStateChanged);
        }
        return writer;
    },
    decode(input, length) {
        const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
        let end = length === undefined ? reader.len : reader.pos + length;
        const message = createBaseNotification();
        while (reader.pos < end) {
            const tag = reader.uint32();
            switch (tag >>> 3) {
                case 1:
                    if (tag !== 8) {
                        break;
                    }
                    message.lockStateChanged = reader.int32();
                    continue;
            }
            if ((tag & 7) === 4 || tag === 0) {
                break;
            }
            reader.skipType(tag & 7);
        }
        return message;
    },
    fromJSON(object) {
        return {
            lockStateChanged: isSet(object.lockStateChanged) ? lockStateFromJSON(object.lockStateChanged) : undefined,
        };
    },
    toJSON(message) {
        const obj = {};
        if (message.lockStateChanged !== undefined) {
            obj.lockStateChanged = lockStateToJSON(message.lockStateChanged);
        }
        return obj;
    },
    create(base) {
        return Notification.fromPartial(base ?? {});
    },
    fromPartial(object) {
        const message = createBaseNotification();
        message.lockStateChanged = object.lockStateChanged ?? undefined;
        return message;
    },
};
function bytesFromBase64(b64) {
    const bin = globalThis.atob(b64);
    const arr = new Uint8Array(bin.length);
    for (let i = 0; i < bin.length; ++i) {
        arr[i] = bin.charCodeAt(i);
    }
    return arr;
}
function base64FromBytes(arr) {
    const bin = [];
    arr.forEach((byte) => {
        bin.push(globalThis.String.fromCharCode(byte));
    });
    return globalThis.btoa(bin.join(""));
}
function isSet(value) {
    return value !== null && value !== undefined;
}
