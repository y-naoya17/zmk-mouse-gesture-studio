// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v1.180.0
//   protoc               v7.34.1
// source: custom.proto
/* eslint-disable */
import * as _m0 from "protobufjs/minimal";
export const protobufPackage = "zmk.custom";
function createBaseListCustomSubsystemRequest() {
    return {};
}
export const ListCustomSubsystemRequest = {
    encode(_, writer = _m0.Writer.create()) {
        return writer;
    },
    decode(input, length) {
        const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
        let end = length === undefined ? reader.len : reader.pos + length;
        const message = createBaseListCustomSubsystemRequest();
        while (reader.pos < end) {
            const tag = reader.uint32();
            switch (tag >>> 3) {
            }
            if ((tag & 7) === 4 || tag === 0) {
                break;
            }
            reader.skipType(tag & 7);
        }
        return message;
    },
    fromJSON(_) {
        return {};
    },
    toJSON(_) {
        const obj = {};
        return obj;
    },
    create(base) {
        return ListCustomSubsystemRequest.fromPartial(base ?? {});
    },
    fromPartial(_) {
        const message = createBaseListCustomSubsystemRequest();
        return message;
    },
};
function createBaseListCustomSubsystemResponse() {
    return { subsystems: [] };
}
export const ListCustomSubsystemResponse = {
    encode(message, writer = _m0.Writer.create()) {
        for (const v of message.subsystems) {
            CustomSubsystemInfo.encode(v, writer.uint32(10).fork()).ldelim();
        }
        return writer;
    },
    decode(input, length) {
        const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
        let end = length === undefined ? reader.len : reader.pos + length;
        const message = createBaseListCustomSubsystemResponse();
        while (reader.pos < end) {
            const tag = reader.uint32();
            switch (tag >>> 3) {
                case 1:
                    if (tag !== 10) {
                        break;
                    }
                    message.subsystems.push(CustomSubsystemInfo.decode(reader, reader.uint32()));
                    continue;
            }
            if ((tag & 7) === 4 || tag === 0) {
                break;
            }
            reader.skipType(tag & 7);
        }
        return message;
    },
    fromJSON(object) {
        return {
            subsystems: globalThis.Array.isArray(object?.subsystems)
                ? object.subsystems.map((e) => CustomSubsystemInfo.fromJSON(e))
                : [],
        };
    },
    toJSON(message) {
        const obj = {};
        if (message.subsystems?.length) {
            obj.subsystems = message.subsystems.map((e) => CustomSubsystemInfo.toJSON(e));
        }
        return obj;
    },
    create(base) {
        return ListCustomSubsystemResponse.fromPartial(base ?? {});
    },
    fromPartial(object) {
        const message = createBaseListCustomSubsystemResponse();
        message.subsystems = object.subsystems?.map((e) => CustomSubsystemInfo.fromPartial(e)) || [];
        return message;
    },
};
function createBaseCustomSubsystemInfo() {
    return { index: 0, identifier: "", uiUrl: [] };
}
export const CustomSubsystemInfo = {
    encode(message, writer = _m0.Writer.create()) {
        if (message.index !== 0) {
            writer.uint32(8).uint32(message.index);
        }
        if (message.identifier !== "") {
            writer.uint32(18).string(message.identifier);
        }
        for (const v of message.uiUrl) {
            writer.uint32(26).string(v);
        }
        return writer;
    },
    decode(input, length) {
        const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
        let end = length === undefined ? reader.len : reader.pos + length;
        const message = createBaseCustomSubsystemInfo();
        while (reader.pos < end) {
            const tag = reader.uint32();
            switch (tag >>> 3) {
                case 1:
                    if (tag !== 8) {
                        break;
                    }
                    message.index = reader.uint32();
                    continue;
                case 2:
                    if (tag !== 18) {
                        break;
                    }
                    message.identifier = reader.string();
                    continue;
                case 3:
                    if (tag !== 26) {
                        break;
                    }
                    message.uiUrl.push(reader.string());
                    continue;
            }
            if ((tag & 7) === 4 || tag === 0) {
                break;
            }
            reader.skipType(tag & 7);
        }
        return message;
    },
    fromJSON(object) {
        return {
            index: isSet(object.index) ? globalThis.Number(object.index) : 0,
            identifier: isSet(object.identifier) ? globalThis.String(object.identifier) : "",
            uiUrl: globalThis.Array.isArray(object?.uiUrl) ? object.uiUrl.map((e) => globalThis.String(e)) : [],
        };
    },
    toJSON(message) {
        const obj = {};
        if (message.index !== 0) {
            obj.index = Math.round(message.index);
        }
        if (message.identifier !== "") {
            obj.identifier = message.identifier;
        }
        if (message.uiUrl?.length) {
            obj.uiUrl = message.uiUrl;
        }
        return obj;
    },
    create(base) {
        return CustomSubsystemInfo.fromPartial(base ?? {});
    },
    fromPartial(object) {
        const message = createBaseCustomSubsystemInfo();
        message.index = object.index ?? 0;
        message.identifier = object.identifier ?? "";
        message.uiUrl = object.uiUrl?.map((e) => e) || [];
        return message;
    },
};
function createBaseCallRequest() {
    return { subsystemIndex: 0, payload: new Uint8Array(0) };
}
export const CallRequest = {
    encode(message, writer = _m0.Writer.create()) {
        if (message.subsystemIndex !== 0) {
            writer.uint32(8).uint32(message.subsystemIndex);
        }
        if (message.payload.length !== 0) {
            writer.uint32(18).bytes(message.payload);
        }
        return writer;
    },
    decode(input, length) {
        const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
        let end = length === undefined ? reader.len : reader.pos + length;
        const message = createBaseCallRequest();
        while (reader.pos < end) {
            const tag = reader.uint32();
            switch (tag >>> 3) {
                case 1:
                    if (tag !== 8) {
                        break;
                    }
                    message.subsystemIndex = reader.uint32();
                    continue;
                case 2:
                    if (tag !== 18) {
                        break;
                    }
                    message.payload = reader.bytes();
                    continue;
            }
            if ((tag & 7) === 4 || tag === 0) {
                break;
            }
            reader.skipType(tag & 7);
        }
        return message;
    },
    fromJSON(object) {
        return {
            subsystemIndex: isSet(object.subsystemIndex) ? globalThis.Number(object.subsystemIndex) : 0,
            payload: isSet(object.payload) ? bytesFromBase64(object.payload) : new Uint8Array(0),
        };
    },
    toJSON(message) {
        const obj = {};
        if (message.subsystemIndex !== 0) {
            obj.subsystemIndex = Math.round(message.subsystemIndex);
        }
        if (message.payload.length !== 0) {
            obj.payload = base64FromBytes(message.payload);
        }
        return obj;
    },
    create(base) {
        return CallRequest.fromPartial(base ?? {});
    },
    fromPartial(object) {
        const message = createBaseCallRequest();
        message.subsystemIndex = object.subsystemIndex ?? 0;
        message.payload = object.payload ?? new Uint8Array(0);
        return message;
    },
};
function createBaseCallResponse() {
    return { subsystemIndex: 0, payload: new Uint8Array(0) };
}
export const CallResponse = {
    encode(message, writer = _m0.Writer.create()) {
        if (message.subsystemIndex !== 0) {
            writer.uint32(8).uint32(message.subsystemIndex);
        }
        if (message.payload.length !== 0) {
            writer.uint32(18).bytes(message.payload);
        }
        return writer;
    },
    decode(input, length) {
        const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
        let end = length === undefined ? reader.len : reader.pos + length;
        const message = createBaseCallResponse();
        while (reader.pos < end) {
            const tag = reader.uint32();
            switch (tag >>> 3) {
                case 1:
                    if (tag !== 8) {
                        break;
                    }
                    message.subsystemIndex = reader.uint32();
                    continue;
                case 2:
                    if (tag !== 18) {
                        break;
                    }
                    message.payload = reader.bytes();
                    continue;
            }
            if ((tag & 7) === 4 || tag === 0) {
                break;
            }
            reader.skipType(tag & 7);
        }
        return message;
    },
    fromJSON(object) {
        return {
            subsystemIndex: isSet(object.subsystemIndex) ? globalThis.Number(object.subsystemIndex) : 0,
            payload: isSet(object.payload) ? bytesFromBase64(object.payload) : new Uint8Array(0),
        };
    },
    toJSON(message) {
        const obj = {};
        if (message.subsystemIndex !== 0) {
            obj.subsystemIndex = Math.round(message.subsystemIndex);
        }
        if (message.payload.length !== 0) {
            obj.payload = base64FromBytes(message.payload);
        }
        return obj;
    },
    create(base) {
        return CallResponse.fromPartial(base ?? {});
    },
    fromPartial(object) {
        const message = createBaseCallResponse();
        message.subsystemIndex = object.subsystemIndex ?? 0;
        message.payload = object.payload ?? new Uint8Array(0);
        return message;
    },
};
function createBaseCustomNotification() {
    return { subsystemIndex: 0, payload: new Uint8Array(0) };
}
export const CustomNotification = {
    encode(message, writer = _m0.Writer.create()) {
        if (message.subsystemIndex !== 0) {
            writer.uint32(8).uint32(message.subsystemIndex);
        }
        if (message.payload.length !== 0) {
            writer.uint32(18).bytes(message.payload);
        }
        return writer;
    },
    decode(input, length) {
        const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
        let end = length === undefined ? reader.len : reader.pos + length;
        const message = createBaseCustomNotification();
        while (reader.pos < end) {
            const tag = reader.uint32();
            switch (tag >>> 3) {
                case 1:
                    if (tag !== 8) {
                        break;
                    }
                    message.subsystemIndex = reader.uint32();
                    continue;
                case 2:
                    if (tag !== 18) {
                        break;
                    }
                    message.payload = reader.bytes();
                    continue;
            }
            if ((tag & 7) === 4 || tag === 0) {
                break;
            }
            reader.skipType(tag & 7);
        }
        return message;
    },
    fromJSON(object) {
        return {
            subsystemIndex: isSet(object.subsystemIndex) ? globalThis.Number(object.subsystemIndex) : 0,
            payload: isSet(object.payload) ? bytesFromBase64(object.payload) : new Uint8Array(0),
        };
    },
    toJSON(message) {
        const obj = {};
        if (message.subsystemIndex !== 0) {
            obj.subsystemIndex = Math.round(message.subsystemIndex);
        }
        if (message.payload.length !== 0) {
            obj.payload = base64FromBytes(message.payload);
        }
        return obj;
    },
    create(base) {
        return CustomNotification.fromPartial(base ?? {});
    },
    fromPartial(object) {
        const message = createBaseCustomNotification();
        message.subsystemIndex = object.subsystemIndex ?? 0;
        message.payload = object.payload ?? new Uint8Array(0);
        return message;
    },
};
function createBaseRequest() {
    return { listCustomSubsystems: undefined, call: undefined };
}
export const Request = {
    encode(message, writer = _m0.Writer.create()) {
        if (message.listCustomSubsystems !== undefined) {
            ListCustomSubsystemRequest.encode(message.listCustomSubsystems, writer.uint32(10).fork()).ldelim();
        }
        if (message.call !== undefined) {
            CallRequest.encode(message.call, writer.uint32(18).fork()).ldelim();
        }
        return writer;
    },
    decode(input, length) {
        const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
        let end = length === undefined ? reader.len : reader.pos + length;
        const message = createBaseRequest();
        while (reader.pos < end) {
            const tag = reader.uint32();
            switch (tag >>> 3) {
                case 1:
                    if (tag !== 10) {
                        break;
                    }
                    message.listCustomSubsystems = ListCustomSubsystemRequest.decode(reader, reader.uint32());
                    continue;
                case 2:
                    if (tag !== 18) {
                        break;
                    }
                    message.call = CallRequest.decode(reader, reader.uint32());
                    continue;
            }
            if ((tag & 7) === 4 || tag === 0) {
                break;
            }
            reader.skipType(tag & 7);
        }
        return message;
    },
    fromJSON(object) {
        return {
            listCustomSubsystems: isSet(object.listCustomSubsystems)
                ? ListCustomSubsystemRequest.fromJSON(object.listCustomSubsystems)
                : undefined,
            call: isSet(object.call) ? CallRequest.fromJSON(object.call) : undefined,
        };
    },
    toJSON(message) {
        const obj = {};
        if (message.listCustomSubsystems !== undefined) {
            obj.listCustomSubsystems = ListCustomSubsystemRequest.toJSON(message.listCustomSubsystems);
        }
        if (message.call !== undefined) {
            obj.call = CallRequest.toJSON(message.call);
        }
        return obj;
    },
    create(base) {
        return Request.fromPartial(base ?? {});
    },
    fromPartial(object) {
        const message = createBaseRequest();
        message.listCustomSubsystems = (object.listCustomSubsystems !== undefined && object.listCustomSubsystems !== null)
            ? ListCustomSubsystemRequest.fromPartial(object.listCustomSubsystems)
            : undefined;
        message.call = (object.call !== undefined && object.call !== null)
            ? CallRequest.fromPartial(object.call)
            : undefined;
        return message;
    },
};
function createBaseResponse() {
    return { listCustomSubsystems: undefined, call: undefined };
}
export const Response = {
    encode(message, writer = _m0.Writer.create()) {
        if (message.listCustomSubsystems !== undefined) {
            ListCustomSubsystemResponse.encode(message.listCustomSubsystems, writer.uint32(10).fork()).ldelim();
        }
        if (message.call !== undefined) {
            CallResponse.encode(message.call, writer.uint32(18).fork()).ldelim();
        }
        return writer;
    },
    decode(input, length) {
        const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
        let end = length === undefined ? reader.len : reader.pos + length;
        const message = createBaseResponse();
        while (reader.pos < end) {
            const tag = reader.uint32();
            switch (tag >>> 3) {
                case 1:
                    if (tag !== 10) {
                        break;
                    }
                    message.listCustomSubsystems = ListCustomSubsystemResponse.decode(reader, reader.uint32());
                    continue;
                case 2:
                    if (tag !== 18) {
                        break;
                    }
                    message.call = CallResponse.decode(reader, reader.uint32());
                    continue;
            }
            if ((tag & 7) === 4 || tag === 0) {
                break;
            }
            reader.skipType(tag & 7);
        }
        return message;
    },
    fromJSON(object) {
        return {
            listCustomSubsystems: isSet(object.listCustomSubsystems)
                ? ListCustomSubsystemResponse.fromJSON(object.listCustomSubsystems)
                : undefined,
            call: isSet(object.call) ? CallResponse.fromJSON(object.call) : undefined,
        };
    },
    toJSON(message) {
        const obj = {};
        if (message.listCustomSubsystems !== undefined) {
            obj.listCustomSubsystems = ListCustomSubsystemResponse.toJSON(message.listCustomSubsystems);
        }
        if (message.call !== undefined) {
            obj.call = CallResponse.toJSON(message.call);
        }
        return obj;
    },
    create(base) {
        return Response.fromPartial(base ?? {});
    },
    fromPartial(object) {
        const message = createBaseResponse();
        message.listCustomSubsystems = (object.listCustomSubsystems !== undefined && object.listCustomSubsystems !== null)
            ? ListCustomSubsystemResponse.fromPartial(object.listCustomSubsystems)
            : undefined;
        message.call = (object.call !== undefined && object.call !== null)
            ? CallResponse.fromPartial(object.call)
            : undefined;
        return message;
    },
};
function createBaseNotification() {
    return { customNotification: undefined };
}
export const Notification = {
    encode(message, writer = _m0.Writer.create()) {
        if (message.customNotification !== undefined) {
            CustomNotification.encode(message.customNotification, writer.uint32(10).fork()).ldelim();
        }
        return writer;
    },
    decode(input, length) {
        const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
        let end = length === undefined ? reader.len : reader.pos + length;
        const message = createBaseNotification();
        while (reader.pos < end) {
            const tag = reader.uint32();
            switch (tag >>> 3) {
                case 1:
                    if (tag !== 10) {
                        break;
                    }
                    message.customNotification = CustomNotification.decode(reader, reader.uint32());
                    continue;
            }
            if ((tag & 7) === 4 || tag === 0) {
                break;
            }
            reader.skipType(tag & 7);
        }
        return message;
    },
    fromJSON(object) {
        return {
            customNotification: isSet(object.customNotification)
                ? CustomNotification.fromJSON(object.customNotification)
                : undefined,
        };
    },
    toJSON(message) {
        const obj = {};
        if (message.customNotification !== undefined) {
            obj.customNotification = CustomNotification.toJSON(message.customNotification);
        }
        return obj;
    },
    create(base) {
        return Notification.fromPartial(base ?? {});
    },
    fromPartial(object) {
        const message = createBaseNotification();
        message.customNotification = (object.customNotification !== undefined && object.customNotification !== null)
            ? CustomNotification.fromPartial(object.customNotification)
            : undefined;
        return message;
    },
};
function bytesFromBase64(b64) {
    const bin = globalThis.atob(b64);
    const arr = new Uint8Array(bin.length);
    for (let i = 0; i < bin.length; ++i) {
        arr[i] = bin.charCodeAt(i);
    }
    return arr;
}
function base64FromBytes(arr) {
    const bin = [];
    arr.forEach((byte) => {
        bin.push(globalThis.String.fromCharCode(byte));
    });
    return globalThis.btoa(bin.join(""));
}
function isSet(value) {
    return value !== null && value !== undefined;
}
